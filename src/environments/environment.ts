// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  firebase: {
    apiKey: "AIzaSyCVJQ5U1b3ZM80f94J26ZDgmEBweBsSlJU",
    authDomain: "ninemiles-project.firebaseapp.com",
    databaseURL: "https://ninemiles-project.firebaseio.com",
    projectId: "ninemiles-project",
    storageBucket: "ninemiles-project.appspot.com",
    messagingSenderId: "297664511765",
    appId: "1:297664511765:web:b91e65193c2d4b99a5f31c"
  }
};


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
